rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Editable site content — anyone can read, only signed-in admins can write.
    match /content/{doc=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
